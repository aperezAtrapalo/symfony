// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("makhan_debug", "for makhan_debug support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("makhan_debug", "enable makhan_debug support", "no");

if (PHP_SYMFONY_DEBUG != "no") {
	EXTENSION("makhan_debug", "makhan_debug.c");
}

